[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "graycode-router-sdk"
version = "0.0.1"
description = "GraycodeRouter conversation DAG HTTP API client"
requires-python = ">=3.10"
dependencies = ["httpx"]
license = {text = "MIT"}
