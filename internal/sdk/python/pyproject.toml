[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "eyrie-sdk"
version = "0.6.0"
description = "Eyrie conversation DAG HTTP API client"
requires-python = ">=3.10"
dependencies = ["httpx"]
license = {text = "MIT"}
