// Package config holds eyrie's provider configuration model and accessors for
// the active provider/model selection (ActiveProvider, ActiveModel,
// SetActiveProvider, SetProviderModel) used to route requests.
package config
