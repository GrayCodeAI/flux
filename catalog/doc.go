// Package catalog defines graycode-router's model catalog: the versioned set of known
// providers and their model entries, the bootstrap (built-in) catalog, and
// helpers to compile and query catalog data (e.g. resolving model entries for
// a given provider).
package catalog
