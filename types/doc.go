// Package types holds graycode-router's shared data types — message/content blocks and
// related request/response shapes — plus small cross-cutting helpers such as
// retry backoff (BackoffDelay, CryptoRandDuration).
package types
